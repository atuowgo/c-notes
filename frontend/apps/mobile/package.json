{
  "name": "@cnotes/mobile",
  "version": "0.0.1",
  "private": true,
  "description": "知识炼金炉移动端:Capacitor 包壳 Web 阅读端(复用 @cnotes/web 产物)",
  "scripts": {
    "build:web": "pnpm --filter @cnotes/web build",
    "sync": "pnpm build:web && cap sync",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "@capacitor/android": "^6.2.0"
  }
}
